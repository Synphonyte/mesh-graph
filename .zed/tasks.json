// Project tasks configuration. See https://zed.dev/docs/tasks for documentation.
//
// Example:
[
	{
		"label": "Run vn.rs",
		"command": "cargo",
		"args": [
			"run",
			"--bin",
			"vertex-neighborhood",
			"--features",
			"rerun,gltf,tracing-subscriber"
		],
		// Env overrides for the command, will be appended to the terminal's environment from the settings.
		"env": { "RUST_LOG": "info", "RUST_BACKTRACE": "1" }
	},
	{
		"label": "Test vertex_neighborhood",
		"command": "cargo",
		"args": [
			"test",
			"test_remove_double_flaps",
			"--lib",
			// "--release",
			"--features",
			"rerun",
			"--",
			"--test-threads=1"
		],
		// Env overrides for the command, will be appended to the terminal's environment from the settings.
		"env": { "RUST_LOG": "info", "RUST_BACKTRACE": "1" }
	},
	{
		"label": "Test merge one-ring",
		"command": "cargo",
		"args": [
			"test",
			"test_vertex_merge_common_vert_he_tube",
			// "ops::merge_one_ring",
			"--lib",
			// "--release",
			"--features",
			"rerun,gltf",
			"--",
			"--test-threads=1"
		],
		// Env overrides for the command, will be appended to the terminal's environment from the settings.
		"env": { "RUST_LOG": "info", "RUST_BACKTRACE": "1" }
	},
	{
		"label": "Test collapse operations",
		"command": "cargo",
		"args": [
			"test",
			"ops::collapse",
			"--lib",
			// "--release",
			"--features",
			"rerun,gltf",
			"--",
			"--test-threads=1"
		],
		// Env overrides for the command, will be appended to the terminal's environment from the settings.
		"env": { "RUST_LOG": "info", "RUST_BACKTRACE": "1" }
	}
]
